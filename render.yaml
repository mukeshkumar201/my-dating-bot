services:
  - type: worker
    name: dating-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dating_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
